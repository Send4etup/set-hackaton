services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - QWEN_BASE_URL=http://10.93.26.100:42005/v1
      - QWEN_API_KEY=1234567890
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped
